name: Environment Variables

on:
  push:
    branches:
      - main

env: 
  GLOBAL_VAR: Hello, World!
jobs: 
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Output Global Variable
        run: echo ${GLOBAL_VAR}
  second_job:
    runs-on: ubuntu-latest
    steps:
      - name: Output Global variable 2 
        run: echo ${GLOBAL_VAR}

