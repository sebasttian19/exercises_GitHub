name: Use actions

on:
  push:
    branches:
      - main

jobs:
  actions:
    name: Run actions
    runs-on: ubuntu-latest
    steps: 
      - name: List directory contents
        run: ls -ltra
      - name: Checkout repository  
        uses: actions/checkout@v3
      - name: 22 List directory contests 22
        run: ls -ltra
